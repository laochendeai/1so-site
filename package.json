{
  "name": "1so-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "audit:geo": "node scripts/geo-audit.mjs",
    "deploy": "wrangler pages deploy public --project-name=1so-landing --branch=main"
  },
  "devDependencies": {
    "wrangler": "^4.14.0"
  }
}
